name: Security

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 2 * * 1'
  workflow_dispatch:

jobs:
  security:
    uses: brooksmcmillin/workflows/.github/workflows/node-security.yml@main
    with:
      node-version: '22'

  claude-security:
    uses: brooksmcmillin/workflows/.github/workflows/claude-security-review.yml@main
    secrets:
      CLAUDE_API_KEY: ${{ secrets.CLAUDE_API_KEY }}
